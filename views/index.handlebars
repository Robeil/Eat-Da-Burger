<div class="container">
    <div class="">
        <img src="http://image.flaticon.com/icons/png/512/189/189127.png" class="mx-auto d-block" alt="Burger img"
            width="200" height="200">
    </div>
    <div class="text-center">
        <h1>Eat-Da-Burger</h1>
    </div>
    <div>
        <section>
            <div class="text-center">
                 <div >
                <form>
                    <textarea type="text" name="burger" id="burger"></textarea>
                    <br>
                    <button id="burger" type="submit" class="btn btn-info" >Enter Burger!</button>
                </form>
                   </div>
            </div>
        </section>
    </div>
    <div>
        <aside>
            <div class="">
            <h2>Burgers be Eat:</h2>
            </div>
            <dl>
                {{#each burgers}}
                <dt>
                    <p>
                        {{this.id}} {{this.burger_name}}
                    </p>
                    <button data-burgerId="{{this.id}}" class="btn btn-info">Devour Please!</button>
                </dt>
                {{/each}}
            </dl>
        </aside>
    </div>

    <div>
        <aside>
            <div>
            <h2>Devoured Burgers</h2>
            </div>
            <dl>
                {{#each burgers}}
                {{#if devoured}}
                <dt class="">
                    <p class="float-left">
                        {{this.id}}. {{this.burger_name}}
                    </p>
                </dt>
                {{/if}}
                {{/each}}
            </dl>
        </aside>
    </div>
</div>